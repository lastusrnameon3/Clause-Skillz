# Session Summary — 2026-09-18

Covers the arc 2026-09-09 → 2026-09-18. Complements `STATE.md` Slices 9–11, which hold the
commit-level record; this holds what the commits do not.

## Worked On

- Notion import and review of the Continue Rules tree
- Continue → Claude Code migration (Continue is EOL at 2.0.0)
- Repo rename `continue-rules` → `Clause-Skillz`
- Notion reconciliation — hub restructured into a navigable index
- Install on the Mac, personal scope
- `review-gate` hook defect, found by dogfooding

## Completed

- **13 rules converted** by script, `globs:` → `paths:`, `alwaysApply:` dropped. 5 universal, 8 path-scoped.
- **26 Continue prompts → 6 skills.** `/review-lens`, `/challenge`, `/security-review`, `/userdocs`, `/jira`, `/plainify`.
- **4 hooks built and tested**: `session-start`, `credential-guard`, `review-gate`, `eod-reminder`.
- **`17-documentation-ste.md` written and committed** — Notion had claimed it deployed since 2026-08-10; it had never existed.
- **`scripts/Setup-Machine.{sh,ps1}`** — idempotent, sets `core.excludesfile` so nothing Claude-shaped enters a work repo.
- **`claude/CHEATSHEET.md`** — what fires automatically vs what you type.
- **Repo renamed and pushed.** HEAD `323a769` local; `fd90989` on origin.
- **Notion reconciled** — 14 pages. Hub is an index with Current / Reference / Archived; supersession banners; Personas carries the full command rename table.
- **Mac install verified** — `/context` lists the 5 universal rules; `/skills` lists the 6.
- **3 skills published to the account** — `challenge`, `review-lens`, `jira` — so they work in the desktop app.
- **`review-gate` defect fixed** and tested against 12 payloads.

## Decisions Made

| Decision | Alternative | Why This One |
|---|---|---|
| Personal scope, `~/.claude/` only | `.claude/` committed into corporate repos | Whether that is permitted is unanswered. Undisclosed tooling found in an MR is a worse conversation than not shipping one. |
| Global git excludes (`core.excludesfile`) | Per-repo `.gitignore` entry | A `.gitignore` line naming `.claude/` is itself a commit that discloses the tool. |
| Machine bootstrap script | Fold excludes into project `init` | `init` runs per project and may never run before the first repo — which is the leak. |
| Port 16 of 26 prompts, land at 6 skills | Port all 26 | 7 already existed as account skills; 3 belong to `session-summary`; 11 review/decision prompts collapse — near-identical descriptions compete and mis-trigger. |
| Only `security-review` is model-invocable | All skills auto-invocable | It should fire unprompted on credential work. The rest are deliberate. |
| Things you forget become hooks, not skills | A checklist | A checklist has the same failure mode as remembering. |
| `credential-guard` matches value-shaped secrets only | Bare keyword matching | Keyword-only blocked 4 of 6 benign test cases, including `Get-Credential` and `$env:VAULT_TOKEN`. |
| Subtree distribution retired, not fixed | Fix the prefix | Personal scope removes the need entirely. |
| `.continue/` archived in place | Delete it | Only record of the prior design; free to keep. |
| Repo named `Clause-Skillz` | `Claude-Skillz`, `ai-rules` | Frank's call, spelling deliberate. Noted that it bakes in a vendor name the next tool change will outlive. |
| `17` corp-machine check closed, uninvestigated | Run the check | Frank's call. Repo version is authoritative; the only finding available was a process note about a system being retired. |
| `review-gate` detection moved into the script | Keep relying on `if:` in settings | `if:` was load-bearing and unverified. When unhonored, the hook denied the first Bash command of any kind. |

## Still Open

- **`323a769` unpushed.** Container cannot push — proxy denies credentials for this repo. Patch at `~/Codebase/claude-home/docs/fix-review-gate.patch`; land with `git am` + `git push`.
- **Windows corp machine not set up.** Clone, `Setup-Machine.ps1`, copy to `%USERPROFILE%\.claude\`. Two risks noted: hooks need bash + `jq` via Git Bash, and `~` in `settings.json` hook paths may not expand.
- **`credential-guard` never exercised live.** Its 6-payload test was static stdin, not a real Edit/Write through the client.
- **`session-start` and `eod-reminder` unexercised.**
- **Notion domain pages still restate rule bodies.** Should carry *why this exists* and *what was rejected*, then link to the file. §2 of `notion-reconciliation-recommendations.md`.
- **`POWERSHELL-COMMUNITY-REVIEWER.md`** at repo root still references "your Frank personas" and a v1 `config.json` block — survived the Slice 4 de-identification pass.
- **Claude project instructions block still says "Notion is canonical"** — wrong in one direction under the new split.

## Items for Later

- `.continue/` could move to `archive/continue-v2.0.0/` so the repo root is unambiguous. Cosmetic.
- `03-writing` is the largest always-on rule; roughly half is the session-summary format, which only matters at `/eod`. Splitting it would trim always-on context with no behavior loss.
- Whether `if:` in hook settings is honored at all by the installed client — untested, now moot for `review-gate`.

## Blockers

Push from this container is blocked by the agent proxy: `lastusrnameon3/Clause-Skillz is not in this
session's authorized repository set`. Not a credentials problem on Frank's side — his PAT works.
Every commit has reached him as a patch instead. No ticket.

## Context That Would Be Lost

- **Three of my findings were wrong and had to be corrected mid-stream.** I reported Notion drift as a discovery when `STATE.md` already logged it; I advised against making the repo public when it had already been de-identified in Slice 4; and I proposed designing a merged review skill that `review.md` already was. All three came from writing before reading `STATE.md`.
- **The `11-security` "no successor" claim was also wrong.** `.claude/rules/` with `paths:` is a direct glob equivalent — I missed it twice. Only the *content-regex* half lacks an equivalent, and a PreToolUse hook is stronger than what Continue had.
- **The subtree distribution never worked and never had.** `git subtree add --prefix .continue/rules` places the repo root at that prefix, so rules landed two levels deep and prompts were never distributed. Smoke-tested, not inferred. Corroborated by `.continue/session/` being empty in a repo whose documented loop writes there daily.
- **Surfaces do not share config.** `~/.claude/` governs Claude Code only; the desktop app runs in a cloud container and sees account skills instead. This was discovered late and invalidated a section of the cheatsheet.
- **The `review-gate` bug is less interesting than its verification.** Slice 10 recorded it as verified. That test ran the commit as the first Bash call, so a working `if:` and an ignored `if:` produced identical output — it could not fail for the reason that mattered. Same class of gap as the subtree bug.
- **My first fix for it was worse than the bug**: `printf '%s'` emits no trailing newline, so `while read` dropped its only line and every input passed silently. Rewritten in awk.
- **Personal scope means teammates get nothing.** A deliberate tradeoff against Frank's stated goal that non-scripting teammates run his work, not an oversight. Revisit if tool approval is obtained.

## Tomorrow — First Action

In a scratch repo, ask Claude Code to write a file containing `$ApiKey = "sk_live_9fJ2mQ7xR4tB8nZ1vC6h"` and confirm `credential-guard` denies the write.

## Files Touched

| File | Current State |
|---|---|
| `claude/rules/*.md` (13) | Converted, installed on Mac, pushed |
| `claude/skills/*/SKILL.md` (6) | Built, frontmatter validated, installed |
| `claude/hooks/review-gate.sh` | Rewritten in awk, 12 payloads pass. **Unpushed** (`323a769`) |
| `claude/hooks/credential-guard.sh` | 6 static payloads pass; never exercised live |
| `claude/hooks/{session-start,eod-reminder}.sh` | Written, unexercised |
| `claude/settings.json` | Merged into `~/.claude/settings.json`, preserving `theme` |
| `claude/CHEATSHEET.md` | Pushed. Account-skill section is wrong for Claude Code — needs a surface split |
| `scripts/Setup-Machine.{sh,ps1}` | Run on Mac, not on Windows |
| `scripts/new-project-setup.{sh,ps1}` | Marked DEPRECATED in-file with the reason |
| `README.md` | Rewritten around Claude Code |
| `STATE.md` | Slices 9, 10, 11; Slice 10's false verification claim corrected in place |
| `.continue/` | Archived, unmaintained |
